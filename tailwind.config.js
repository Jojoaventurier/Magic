/** @type {import('tailwindcss').Config} */
module.exports = {
  theme: {
    extend: {
      animation: {
        'spin-slow': 'spin 4s linear infinite', // Spinner lent avec 4s de durée
      },
    },
  },
  plugins: [],
}
