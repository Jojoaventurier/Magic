{% extends 'base.html.twig' %}


{% block title %}Créer un sujet{% endblock %}

{% block body %}

<div class="flex flex-col w-screen h-screen items-center justify-center">
    <div class="bg-white flex flex-col rounded-lg p-4 shadow-md mb-4 space-y-4">
        <h2 class="text-2xl font-semibold text-red-700 mb-4">Nouveau sujet</h2>
        {{ form_start(topicForm, {'attr': {'class': 'space-y-4'}}) }}

            {{ form_widget(topicForm) }}

        {{ form_end(topicForm) }}
    </div>
</div>

{% endblock %}