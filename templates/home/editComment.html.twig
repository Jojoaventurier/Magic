{% extends 'base.html.twig' %}

{% block title %}Modifier Commentaire{% endblock %}

{% block body %}
<div class="min-h-screen bg-gray-100 flex flex-col items-center justify-center">
    <div class="w-full max-w-2xl px-4 mb-6">
        <div class="bg-white rounded-2xl px-8 py-6 shadow-lg">
            <h3 class="text-xl font-semibold mb-4">Modifier Commentaire</h3>
            {{ form_start(commentForm) }}
                <div class="mb-4">
                    {{ form_row(commentForm.textContent, {'attr': {'class': 'w-full px-4 py-2 border rounded-lg focus:outline focus:outline-red-800'}}) }}
                </div>
                    {{ form_row(commentForm.submit, {'attr': {'style': 'display:none;'}}) }}
                <div class="flex justify-end">
                    <button type="submit" class="bg-red-800 text-white px-6 py-2 rounded-lg hover:bg-red-900 transition duration-300">Mettre à jour</button>
                </div>
            {{ form_end(commentForm) }}
        </div>
    </div>
</div>
{% endblock %}